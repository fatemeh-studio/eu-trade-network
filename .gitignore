# Data (fetched / built locally, never committed)
data/raw/
data/processed/
data/external/
*.duckdb
*.duckdb.wal

# Secrets
.env

# QA / scratch figures
figures/qa/

# Generated interactive network HTML (PyVis)
network_viz/

# Quarto
/_site/
/.quarto/
*_files/
# Executed-notebook cache: holds cell outputs, so keep it out of git for the same
# reason notebook outputs are stripped (see .gitattributes / nbstripout).
/_freeze/

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.venv/
.pre-commit-home/

**/*.quarto_ipynb
